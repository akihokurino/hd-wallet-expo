import { Text, View } from "react-native";
import { NftNavigationProp } from "./Navigation";

interface Props {
  navigation: NftNavigationProp;
}

export const NftListScreen: React.FC<Props> = ({ navigation }) => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};
